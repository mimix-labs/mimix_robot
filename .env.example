# Desarrollo local: no guardar secretos reales en este archivo.
MIMIX_ROBOT_ID=robot-dev-001
MIMIX_MQTT_HOST=localhost
MIMIX_MQTT_PORT=1883
MIMIX_TOPIC_PREFIX=mimix/v1
MIMIX_OFFLINE_DB=data/mimix_robot.db

# Vision nativa de la Jetson. El servicio publica landmarks, no video.
MIMIX_VISION_API_URL=http://127.0.0.1:4000/api/vision/hand-landmarks
MIMIX_CAMERA_INDEX=0
MIMIX_CAMERA_WIDTH=640
MIMIX_CAMERA_HEIGHT=480
MIMIX_VISION_FPS=15
MIMIX_VIDEO_BIND_HOST=127.0.0.1
MIMIX_VIDEO_PORT=8081
MIMIX_VIDEO_FPS=15
MIMIX_VIDEO_JPEG_QUALITY=60
# Para una camara CSI, configurar el pipeline GStreamer en .env.
# MIMIX_CAMERA_PIPELINE=nvarguscamerasrc ! video/x-raw(memory:NVMM),width=640,height=480,framerate=30/1 ! nvvidconv ! video/x-raw,format=BGRx ! videoconvert ! video/x-raw,format=BGR ! appsink drop=true sync=false

# Orquestador de la Jetson. Se puede omitir si mimix_web es hermano de mimix_robot.
# MIMIX_WEB_DIR=/home/jetson/mimix_web
# MIMIX_CHROMIUM_BIN=chromium
# MIMIX_CHROMIUM_PROFILE_DIR=/tmp/mimix-chromium

# Guia de voz ElevenLabs. La clave se configura solo en .env local, nunca aqui.
MIMIX_ELEVENLABS_AGENT_ID=agent_xxx
# ELEVENLABS_API_KEY=replace-with-your-private-key
MIMIX_WEB_URL=http://127.0.0.1:4000
# MIMIX_ROBOT_BRIDGE_TOKEN=replace-with-a-long-local-secret

# Audio local: nombres que devuelve `pactl list short sources` y `pactl list short sinks`.
# Configúralos solo en .env de la Jetson; así Wall-E usa el micrófono y parlante correctos.
# MIMIX_AUDIO_INPUT_SOURCE=alsa_input.usb-...
# MIMIX_AUDIO_OUTPUT_SINK=bluez_output....
